mixin link(title, href, text)
  - if (typeof (text) === 'undefined') text = title
  - if (typeof (href) === 'undefined') href = '#'
  a.energy-link(href=href, title=title)&attributes(attributes)
    if block
      block
    else
      != text
